import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createDocumentLink', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
