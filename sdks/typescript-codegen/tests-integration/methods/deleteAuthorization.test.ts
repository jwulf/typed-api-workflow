import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';
describe('deleteAuthorization', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
