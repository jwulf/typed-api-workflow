import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createAuthorization', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
