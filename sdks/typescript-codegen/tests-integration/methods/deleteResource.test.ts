import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';
describe('deleteResource', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
