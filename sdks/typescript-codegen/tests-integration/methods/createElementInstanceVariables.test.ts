import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createElementInstanceVariables', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
