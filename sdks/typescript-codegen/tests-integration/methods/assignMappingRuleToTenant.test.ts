import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('assignMappingRuleToTenant', () => {
  it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ });
});
