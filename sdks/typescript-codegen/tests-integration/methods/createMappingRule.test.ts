import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createMappingRule', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
