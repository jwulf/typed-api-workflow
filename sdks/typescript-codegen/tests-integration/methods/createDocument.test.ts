import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createDocument', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
