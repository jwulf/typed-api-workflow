import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createDeployment', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
