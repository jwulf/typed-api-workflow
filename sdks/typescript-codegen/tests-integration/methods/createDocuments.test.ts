import { describe, it } from 'vitest';
import createCamundaClient from '../../dist';

describe('createDocuments', () => { it('scaffold', () => { const camunda = createCamundaClient(); /* TODO */ }); });
