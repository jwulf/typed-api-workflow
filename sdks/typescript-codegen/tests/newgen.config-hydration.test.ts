import { describe } from 'vitest';

// LEGACY TEST REMOVED: global OpenAPI runtime hydration no longer used (instance-scoped validation & base URL config).

describe.skip('newgen runtime configuration hydration (removed legacy test)', () => {
  /* intentionally empty */
});
