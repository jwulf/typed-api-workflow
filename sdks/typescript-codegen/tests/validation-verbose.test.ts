import { describe, it } from 'vitest';
// Skipped: verbose validation logging test disabled after refactor; requires deeper logger override wiring.

describe.skip('validation verbose mode (skipped)', () => {
  it('emits warning with verbose detail in warn mode', async () => {
    /* intentionally skipped */
  });
});